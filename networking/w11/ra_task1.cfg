!
! Replace 'abcd1234' with your own college username
hostname pull0037-RA

!-- Enable secret password to class
enable secret class

!-- Disable dns lookups
no ip domain-lookup

!
!-- Console logging suppression
line con 0
logging synchronous
exec-timeout 0 0

! VTY access with SSH and Telnet
ip domain-name cnap.cst
username cisco privilege 15 secret cisco
crypto key generate rsa modulus 1024
ip ssh version 2
!
line vty 0 4
transport input telnet ssh
login local
logging synchronous

!-- Enable IPv6 if needed
ipv6 unicast-routing

!-- Allow the router to pick the source IP of the
!-- outgoing interface to the destination.
no ip tftp source-interface

!-- If the device is a switch
!vtp mode transparent

!-- IP configuration
interface Gi0/0/0
    ip address 203.0.113.162 255.255.255.0
    desc Outside interface to Internet
    no shutdown
!
interface Gi0/0/1
    ip address 198.18.162.17 255.255.255.248
    desc Inside interfacem, OSPF neighbour to pull0037-RB
    no shutdown
!

!-- NTP master configuration
ntp master 4

!-- Syslog configuration
logging host 10.162.18.14 transport udp port 514
logging source-interface Gi0/0/0
logging trap informational
service timestamps log datetime msec localtime

!-- Default route to REMOTE network
ip route 0.0.0.0 0.0.0.0 Gi0/0/0 203.0.113.254

!-- OSPF configuration
router ospf 162
    router-id 162.0.0.17
    default-information originate
    auto-cost reference-bandwidth 10000
!
interface Gi0/0/1
    ip ospf 162 area 0
    ip ospf hello-interval 3
    ip ospf dead-interval 6
!
